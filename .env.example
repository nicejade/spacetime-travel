# Copy to `.env` for local overrides (Compose reads this automatically).
# Docker image defaults are already production-safe; only set what you need.

# Host port published by docker compose (maps to container 5168)
PORT=5168

# Override only if you customize the Compose service further.
# Inside the container the app uses:
# SPACETIME_DB_PATH=/app/data/spacetime-travel.sqlite
